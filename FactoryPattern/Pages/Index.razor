@page "/"
@inject ISample1 sample

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>
<h2>@currentTime?.CurrentDateTime</h2>

<button class="btn btn-primary" @onclick="GetNewTime">Get New Time</button>

@code { 
    ISample1? currentTime;
    
    private void GetNewTime()
    {
        currentTime = new Sample1(); // works but are manually creating an instance and therefore taken it out of dependency injection.
    }
}
    